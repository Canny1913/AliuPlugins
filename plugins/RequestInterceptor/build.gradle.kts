version = "0.0.0" // Plugin version. Increment this to trigger an update
description = "Adds ability to intercept Rest API requests/responses." // Plugin description that will be shown to user

aliucord {
    // Changelog of your plugin
    changelog.set(
        """
        """.trimIndent()
    )
    deploy.set(false)
}
