version = "0.0.2"
description = "Displays a ghost icon upon ghosting DMs"

aliucord {
    changelog.set(
        """
        # 0.0.2
        * Fix crash due to bad build config.
        
        # 0.0.1
        * Initial plugin release.
        """.trimIndent()
    )
    deploy.set(true)
}
