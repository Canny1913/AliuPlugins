version = "0.0.1"
description = "Fixes message links not jumping to correct message."

aliucord {
    changelog.set(
        """
        # 0.0.1
        * Initial plugin release.
        """.trimIndent()
    )
    deploy.set(false)
}
