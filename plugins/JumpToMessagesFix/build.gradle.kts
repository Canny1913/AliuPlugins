version = "0.0.1" // Plugin version. Increment this to trigger an update
description = "Fixes message links not jumping to correct message." // Plugin description that will be shown to user

aliucord {
    // Changelog of your plugin
    changelog.set(
        """
        # 0.0.1
        * Initial plugin release.
        """.trimIndent()
    )
    deploy.set(false)
}
